import { HeaderCarouselDirective } from './header-carousel.directive';

describe('HeaderCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
