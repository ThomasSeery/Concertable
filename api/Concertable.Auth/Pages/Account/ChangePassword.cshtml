@page
@model Concertable.Auth.Pages.Account.ChangePasswordModel
@{ ViewData["Title"] = "Change password"; }

<div class="space-y-4">
    <h1 class="text-2xl font-bold text-white">Change your password</h1>

    @if (Model.Success)
    {
        <p class="text-green-400 text-sm">Password updated.</p>
        <a href="/" class="text-purple-400 hover:underline text-sm">Back</a>
    }
    else
    {
        <form method="post" class="space-y-4">
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="text-red-400 text-sm">@Model.ErrorMessage</div>
            }

            <input asp-for="CurrentPassword" type="password"
                   class="w-full px-4 py-2 bg-zinc-800 border border-zinc-700 rounded-lg text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-purple-500"
                   placeholder="Current password" required />
            <input asp-for="NewPassword" type="password"
                   class="w-full px-4 py-2 bg-zinc-800 border border-zinc-700 rounded-lg text-white placeholder-zinc-500 focus:outline-none focus:ring-2 focus:ring-purple-500"
                   placeholder="New password" required minlength="8" />

            <button type="submit"
                    class="w-full py-2 bg-purple-600 hover:bg-purple-700 text-white rounded-lg font-semibold transition">
                Update password
            </button>
        </form>
    }
</div>
