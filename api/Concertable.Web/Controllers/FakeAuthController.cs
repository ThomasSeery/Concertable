// FakeAuthController removed — injected Identity internals (IdentityDbContext, ITokenService)
// which are no longer accessible outside the Identity module boundary.
// Use the real /api/auth/login endpoint with test credentials via IdentityTestSeeder.
