import { MyItemDirective } from '../my-item.directive';

describe('MyItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MyItemDirective();
    expect(directive).toBeTruthy();
  });
});
