import { ExtendedDetailsDirective } from './extended-details.directive';

describe('ExtendedDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendedDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
