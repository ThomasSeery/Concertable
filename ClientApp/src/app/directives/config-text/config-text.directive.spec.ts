import { ConfigTextDirective } from '../config-text.directive';

describe('ConfigTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigTextDirective();
    expect(directive).toBeTruthy();
  });
});
