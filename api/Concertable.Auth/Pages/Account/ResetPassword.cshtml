@page
@model Concertable.Auth.Pages.Account.ResetPasswordModel
@{ Layout = null; }
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Reset password</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-purple-600 to-pink-500 flex items-center justify-center font-sans">
    <form method="post" class="bg-white p-8 rounded-2xl shadow-2xl w-96">
        <h1 class="text-2xl font-bold mb-6 text-gray-800">Set a new password</h1>

        @if (Model.Success)
        {
            <p class="text-green-700 text-sm mb-4">Your password has been reset.</p>
            <a href="/Account/Login" class="text-purple-600 hover:underline text-sm">Sign in</a>
        }
        else
        {
            @if (!string.IsNullOrEmpty(Model.ErrorMessage))
            {
                <div class="text-red-600 text-sm mb-3">@Model.ErrorMessage</div>
            }

            <input asp-for="NewPassword" type="password" class="w-full px-4 py-2 border rounded mb-6 focus:outline-none focus:ring-2 focus:ring-purple-500" placeholder="New password" required minlength="8" />
            <input asp-for="Token" type="hidden" />
            <button type="submit" class="w-full py-2 bg-purple-600 hover:bg-purple-700 text-white rounded font-semibold transition">Reset password</button>
        }
    </form>
</body>
</html>
