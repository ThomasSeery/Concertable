import { DetailsHeroDirective } from './details-hero.directive';

describe('DetailsHeroDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsHeroDirective();
    expect(directive).toBeTruthy();
  });
});
