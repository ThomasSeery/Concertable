import { ItemEventsDirective } from './item-events.directive';

describe('ItemEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemEventsDirective();
    expect(directive).toBeTruthy();
  });
});
